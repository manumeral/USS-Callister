"use strict";

const GAME_CONST = {
    CANVAS : {
        WIDTH : 1080,
        HEIGHT: 1920,
        CONTAINER: "uss-callister"
    },
    STATES: {
        BOOT: "Boot",
        PRELOAD: "Preload"
    }
};

export default GAME_CONST;
